To run, I just press the play button in Intellij. Might take some work to set up gradle and jdk.

Also, I used a local database, so if you also want to run a local database it should be the same, just change
the password in application.properties